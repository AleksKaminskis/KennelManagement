name: Build and deploy .NET Core application to Web App kennel-management-api

on:
  push:
    branches:
      - main

env:
  AZURE_WEBAPP_NAME: kennelca3
  PUBLISH_DIR: publish-server
  CONFIGURATION: Release
  DOTNET_CORE_VERSION: 8.0.x
  WORKING_DIRECTORY: Server

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_CORE_VERSION }}

    - name: Restore
      run: dotnet restore ${{ env.WORKING_DIRECTORY }}

    - name: Build
      run: dotnet build ${{ env.WORKING_DIRECTORY }} --configuration ${{ env.CONFIGURATION }} --no-restore

    - name: Test
      run: dotnet test ${{ env.WORKING_DIRECTORY }} --configuration ${{ env.CONFIGURATION }} --no-build --no-restore

    - name: Publish
      run: |
        dotnet publish ${{ env.WORKING_DIRECTORY }} \
          --configuration ${{ env.CONFIGURATION }} \
          --no-build \
          --output ${{ env.PUBLISH_DIR }}

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: api-app
        path: ${{ env.PUBLISH_DIR }}

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: api-app
        path: ${{ env.PUBLISH_DIR }}

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_KENNELMANAGEMENTAPI }}
        package: ${{ env.PUBLISH_DIR }}
